services:
  node-exporter:
    image: prom/node-exporter:v1.9.1
    container_name: node-exporter
    network_mode: host
    pid: host
    restart: unless-stopped
    command:
      - --path.rootfs=/host
    volumes:
      - /:/host:ro

  prometheus:
    image: prom/prometheus:v3.4.2
    container_name: prometheus
    restart: unless-stopped
    ports:
      - "9091:9090"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    depends_on:
      - node-exporter
      - dummy-exporter

  dummy-exporter:
    build:
      context: ./dummy-exporter
    container_name: dummy-exporter
    restart: unless-stopped
    ports:
      - "9101:9100"

  metric-agent:
    build:
      context: ./metric-agent
    container_name: metric-agent
    restart: unless-stopped
    environment:
      PROMETHEUS_URL: http://prometheus:9090
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4.1-mini}
      DEFAULT_QUERY_SERVER: ${DEFAULT_QUERY_SERVER:-host.docker.internal:9100}
      AGENT_PORT: "8080"
    ports:
      - "8080:8080"
    depends_on:
      - prometheus

volumes:
  prometheus-data:
